<nav class="navbar navbar-expand-lg navbar-light bg-white">
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav-item">
				<a class="nav-link {% if current_tab == 'apercu' %}active{% endif %}" href="{{ path('app_user_appercu') }}">Aperçu</a>
			</li>
			<li class="nav-item">
				<a class="nav-link {% if current_tab == 'deals' %}active{% endif %}" href="{{ path('app_user_deals') }}">Deals
																				({{app.user.deals|length}})</a>
			</li>
			<li class="nav-item">
				<a class="nav-link {% if current_tab == 'sauvegardes' %}active{% endif %}" href="{{ path('app_user_saved') }}">Deals sauvegardés
										({{app.user.favoriteDeals|length}})</a>
			</li>
			<li class="nav-item">
				<a class="nav-link {% if current_tab == 'signal' %}active{% endif %}" href="{{ path('app_user_signal') }}">Deals signalés
										({{app.user.signalements|length}})</a>
			</li>
			<li class="nav-item">
				<a class="nav-link {% if current_tab == 'alertes' %}active{% endif %}" href="{{ path('app_user_alerts') }}">Mes alertes</a>
			</li>
		</ul>
	</div>
</nav>
