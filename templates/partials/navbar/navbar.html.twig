<link rel="stylesheet" href="{{ asset('css/navbar.css') }}">

<nav class="navbar-custom navbar navbar-expand-lg navbar-dark">
	<div class="collapse navbar-collapse navbar-content" id="navbarNavDropdown">
		<a class="navbar-brand" href="/">Dealabs</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="/deals">Bons plans</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/promos">Codes promo</a>
			</li>
		</ul>

		{{ include('partials/navbar/navbar-input-search.html.twig') }}

		<ul class="navbar-nav">
			{% if app.user  %}
				<li class="nav-item">
					<a href="{{ path('app_user_alerts_fil') }}" class="btn btn-primary nav-link btn-connexion">
						<i class="fa fa-bell"></i>
						{% set unseenAlertsCount = 0 %}
						{% for alert in app.user.getAlerts() %}
							{% if not alert.hasBeenShown %}
								{% set unseenAlertsCount = unseenAlertsCount + 1 %}
							{% endif %}
						{% endfor %}
						{% if unseenAlertsCount > 0 %}
							<span class="badge badge-danger">{{ unseenAlertsCount }}</span>
						{% endif %}
					</a>
				</li>
				{{ include('partials/navbar/navbar-dropdown-user.html.twig') }}

			{% else %}
				<li class="nav-item">
					<a href="{{ path('app_login') }}" class="btn btn-primary nav-link btn-connexion">
						<i class="fa fa-user"></i>
						Connexion</a>
				</li>
			{% endif %}
			<li class="nav-item">
				<a href="{{ path('app_deals_add') }}" class="btn btn-primary nav-link btn-ajout">
					<i class="fa fa-plus"></i>
					Deal</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('app_promo_add') }}" class="btn btn-primary nav-link btn-ajout">
					<i class="fa fa-plus"></i>
					Code promo</a>
			</li>
		</ul>
	</div>
</nav></div>
